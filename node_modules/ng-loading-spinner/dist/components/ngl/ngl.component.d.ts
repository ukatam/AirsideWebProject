import { OnDestroy } from '@angular/core';
import { NgLoadingSpinnerService } from '../../services';
export declare class NgLoadingSpinnerComponent implements OnDestroy {
    private progress;
    show: boolean;
    /**
     * Component constructor with injected dependencies.
     * @param progress
     */
    constructor(progress: NgLoadingSpinnerService);
    ngOnDestroy(): void;
}
