import { Injectable } from '@angular/core';
import { NgLoadingSpinnerService } from './ng-loading-spinner.service';
import 'rxjs/add/operator/finally';
var NgLoadingSpinnerInterceptor = /** @class */ (function () {
    function NgLoadingSpinnerInterceptor(spinnerService) {
        this.spinnerService = spinnerService;
        this.requestCounter = 0;
    }
    NgLoadingSpinnerInterceptor.prototype.intercept = function (req, next) {
        var _this = this;
        this.spinnerService.start();
        this.requestCounter++;
        return next.handle(req).finally(function () {
            _this.requestCounter--;
            if (_this.requestCounter === 0) {
                _this.spinnerService.stop();
            }
        });
    };
    NgLoadingSpinnerInterceptor.decorators = [
        { type: Injectable },
    ];
    /** @nocollapse */
    NgLoadingSpinnerInterceptor.ctorParameters = function () { return [
        { type: NgLoadingSpinnerService, },
    ]; };
    return NgLoadingSpinnerInterceptor;
}());
export { NgLoadingSpinnerInterceptor };
//# sourceMappingURL=interceptor.provider.js.map