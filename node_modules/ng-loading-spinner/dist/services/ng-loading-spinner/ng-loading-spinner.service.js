import { Injectable } from '@angular/core';
import { Subject } from 'rxjs/Subject';
/**
 * NgLoadingSpinnerService class.
 */
var NgLoadingSpinnerService = /** @class */ (function () {
    function NgLoadingSpinnerService() {
        /** Progress state */
        this.state = new Subject();
        this._isSpinning = false;
    }
    /** Start spinning */
    NgLoadingSpinnerService.prototype.start = function () {
        if (this.isSpinning()) {
            return;
        }
        this._isSpinning = true;
        this.state.next(true);
    };
    /** Stop spinning */
    NgLoadingSpinnerService.prototype.stop = function () {
        if (!this.isSpinning()) {
            return;
        }
        this._isSpinning = false;
        /** if spinning stop it */
        this.state.next(false);
    };
    NgLoadingSpinnerService.prototype.isSpinning = function () {
        return this._isSpinning;
    };
    NgLoadingSpinnerService.decorators = [
        { type: Injectable },
    ];
    /** @nocollapse */
    NgLoadingSpinnerService.ctorParameters = function () { return []; };
    return NgLoadingSpinnerService;
}());
export { NgLoadingSpinnerService };
//# sourceMappingURL=ng-loading-spinner.service.js.map